import * as React from "react";

/**
 *
 *
 * show error -
 *
 */
//initialState - Error string / null
// const useErrorHandler = (initialState) => {
//    const [error, setError] = React.useState(initialState);

//    //errorMessage - Error string, set error for 3 seconds then set null
//    //component that use hook can render by error value
//    const showError = (errorMessage) => {
//       setError(errorMessage);
//       window.setTimeout(() => {
//          setError(null);
//       }, 3000);
//    };

//    return { error, showError };
// };

// export default useErrorHandler;

// const { error, showError } = useErrorHandler("error string");
